/* Fighting the Heat */
#include <stdio.h>
#define N 40
using namespace std;

int
main(void)
{
	bool h[N][N] = {false};
	int n, m;
	scanf("%d %d", &n, &m);
	for( int i = 0; i < n; i++) {
		for (int j = 0; j < m; j++) {
			String s;
			scanf("%s", s);
	return 0;
}
